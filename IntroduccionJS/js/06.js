// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); // ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); // floor siempre redondea hacia abajo
resultado = Math.sqrt(-200); // sqrt calcula la raiz cuadrada del número
resultado = Math.min(3, 5, 1, 8, 9); // min busca el número menor
resultado = Math.max(3, 5, 1, 8, 9); // max busca el número mayor
resultado = Math.random(); // random genera un número aleatorio
resultado = Math.floor(Math.random() * 30); // Redondear hacia abajo un  
                                            //  número aleatorio multiplicado por 30

console.log(resultado);